name: Stable Release Addon Package

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Addon Zip
        run: |
          set -euo pipefail
          rm -rf dist
          version="${GITHUB_REF_NAME#v}"

          build_package() {
            local flavor_name="$1"
            local toc_source="$2"
            local addon_dir="dist/YouAreNowPrepared-${flavor_name}"
            local zip_name="YouAreNowPrepared-${flavor_name}-${GITHUB_REF_NAME}.zip"

            rm -rf "$addon_dir"
            mkdir -p "$addon_dir/src"
            cp "$toc_source" "$addon_dir/YouAreNowPrepared.toc"
            cp README.md "$addon_dir/"
            cp LICENSE "$addon_dir/"
            cp src/*.lua "$addon_dir/src/"

            sed -i "s/^## Version: .*/## Version: ${version}/" "$addon_dir/YouAreNowPrepared.toc"
            cd dist
            zip -r "../${zip_name}" "YouAreNowPrepared-${flavor_name}"
            cd - >/dev/null
          }

          build_package "ClassicEra" "YouAreNowPrepared_ClassicEra.toc"
          build_package "ClassicTBC" "YouAreNowPrepared_ClassicTBC.toc"

      - name: Publish Stable Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generateReleaseNotes: true
          prerelease: false
          allowUpdates: true
          removeArtifacts: true
          artifacts: YouAreNowPrepared-ClassicEra-${{ github.ref_name }}.zip,YouAreNowPrepared-ClassicTBC-${{ github.ref_name }}.zip
